function setup() {
  let canvas = createCanvas(800, 600);
  canvas.parent("canvasContainer");
  canvas.style("z-index", -1);
  canvas.position(0, 0);
  background(220);
  window.alert("Hello, descendants! Are you still seeing tigers in Korea?");
}

function draw() { 
}